import { NavLink } from "react-router-dom";

export const UserMenu = () => {
    return (
     <div>
        <h1>Helo friend</h1>
        {/* <NavLink to="/contacts">Contacts</NavLink> */}
     </div>

    )
}